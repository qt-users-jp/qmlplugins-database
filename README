$ git clone git://git.qtquick.me/qmlplugins/database.git
$ cd database
$ qmake
$ make
$ (sudo) make install
$ qmlscene ./examples/examples.qml

contributions are always welcome!
see http://qtquick.me/ for details
